namespace Novugit.Commands;

/// <summary>
/// Base settings for config branch commands.
/// This is used for the branch but has no properties.
/// Provides global options to config subcommands.
/// </summary>
public class ConfigSettings : GlobalSettings
{
    // Empty - just provides the global options to config subcommands
}
